The SUBOP frontend is built with React 18 + Vite + TypeScript + Tailwind CSS, consistent with the Design System v1 component specifications and the UI Shell Architecture Plan confirmed in Milestone 2.

All design tokens (colour, typography, spacing) are configured as CSS custom properties in tailwind.config.js and index.css — no hex values are written directly in component files.
